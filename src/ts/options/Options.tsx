import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { OptionsContainer } from './OptionsContainer';

ReactDOM.render(<OptionsContainer />, document.getElementById('options'));
